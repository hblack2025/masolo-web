app = 'masolo-web'
primary_region = 'cdg'

[build]
  dockerfile = 'Dockerfile'

[[services]]
  internal_port = 80
  protocol = 'tcp'
  [[services.ports]]
    handlers = ['tls', 'http']
    port = 443
